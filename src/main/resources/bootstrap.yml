spring:
  cloud:
    config:
      server:
        git:
          uri: ${GIT_REPO_SERVICE_PROPERTIES}
          username: ${GIT_REPO_USERNAME}
          password: ${GIT_REPO_PASSWD}
        bootstrap: true